// // Mobx是一个独立的响应式的库，可以独立于任何UI框架而存在，但是通常人们把他和React
绑定使用，用Mobx来做响应式数据建模，React作为UI视图框架渲染内容
// // 配置需要三个部分
    1、一个通过create-react-app创建好的react项目环境
    2、mobx本身: yarn add mobx
    3、一个链接mobx和react的中间部件: yarn add mobx-react-lite